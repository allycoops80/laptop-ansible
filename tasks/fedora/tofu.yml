---
# ── OpenTofu ──────────────────────────────────────────────────────────
# Fedora ships OpenTofu natively as "opentofu" in its own repos — no
# third-party repo needed (verified against a fedora:44 container).
- name: Install OpenTofu
  dnf:
    name: opentofu
    state: present
