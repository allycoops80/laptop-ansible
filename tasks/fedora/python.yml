---
# ── Python ───────────────────────────────────────────────────────────
# Fedora's python3 package already ships the venv module; only pip needs installing.
- name: Install Python tools
  dnf:
    name:
      - python3-pip
    state: present
